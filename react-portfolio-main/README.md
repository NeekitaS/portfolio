# React + Vite

this project build using react + vite 
to execute or run this project you will need to do following command.

cd react-portfolio-main

if npm not installed 
npm install 
npm run dev 

else
npm run dev 
